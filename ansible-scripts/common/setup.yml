- name: chsh zsh (shell)
  shell: echo
  tags:
    - zsh
    - cli

- name: oh-my-zsh install (git)
  shell: echo
  tags:
    - zsh
    - cli

- name: zsh-autosuggestions install (git)
  shell: echo
  tags:
    - zsh
    - cli

- name: zsh-completions install (git)
  shell: echo
  tags:
    - zsh
    - cli

- name: zsh-syntax-highlighting install (git)
  shell: echo
  tags:
    - zsh
    - cli

- name: zsh-substring-history-search install (git)
  shell: echo
  tags:
    - zsh
    - cli

- name: spaceship-prompt install (git)
  shell: echo
  tags:
    - zsh
    - cli

- name: kitty install (shell)
  shell: curl -L https://sw.kovidgoyal.net/kitty/installer.sh | sh /dev/stdin
  tags:
    - gui
    - kitty

- name: nvm install (shell)
  shell: curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.3/install.sh | bash
  tags:
    - cli
    - node

- name: node install (nvm)
  shell: nvm install lts/fermium lts/gallium
  tags:
    - cli
    - node
    - nvim-lsp
